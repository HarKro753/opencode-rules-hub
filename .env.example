# API key for write operations (POST, PUT, DELETE)
# Change this before deploying!
API_KEY=changeme

# URL where the dashboard can reach the server
# In Docker Compose this should point to the server service
RULES_SERVER_URL=http://server:3847

# Optional: override default ports
# SERVER_PORT=3847
# DASHBOARD_PORT=3848
